 # Руководство по использованию существующих ресурсов

Этот документ описывает, как использовать существующую инфраструктуру DataSphere в Terraform без её пересоздания.

## 🎯 Назначение

Подход с использованием Data Sources позволяет:
- Подключить существующую инфраструктуру DataSphere к Terraform без её пересоздания
- Работать в смешанном режиме (часть ресурсов существующая, часть создается новая)
- Безопасно мигрировать существующие проекты под управление Terraform
- Избежать сложностей с импортом ресурсов в state

## 📋 Поддерживаемые ресурсы

- ✅ **Service Account** - сервисный аккаунт для DataSphere
- ✅ **DataSphere Community** - организационная единица
- ✅ **DataSphere Project** - проект DataSphere
- ✅ **Static Access Key** - ключ доступа к Object Storage

## 🚀 Сценарии использования

### Сценарий 1: Создание новой инфраструктуры (по умолчанию)

```bash
# Просто запустите terraform как обычно
terraform init
terraform plan
terraform apply
```

Все ресурсы будут созданы с нуля.

### Сценарий 2: Полное использование существующей инфраструктуры

1. **Получите ID существующих ресурсов:**

```bash
# Service Account ID
SA_ID=$(yc iam service-account list --format json | jq -r '.[] | select(.name=="datasphere-sa-prod") | .id')
echo "Service Account ID: $SA_ID"

# Community ID
COMMUNITY_ID=$(yc datasphere community list --format json | jq -r '.[] | select(.name=="prod-ds-community") | .id')
echo "Community ID: $COMMUNITY_ID"

# Project ID (замените COMMUNITY_ID на реальный)
PROJECT_ID=$(yc datasphere project list --community-id $COMMUNITY_ID --format json | jq -r '.[] | select(.name=="prod-ds-project") | .id')
echo "Project ID: $PROJECT_ID"

# Static Key ID (опционально)
STATIC_KEY_ID=$(yc iam access-key list --service-account-name datasphere-sa-prod --format json | jq -r '.[0].id')
echo "Static Key ID: $STATIC_KEY_ID"
```

2. **Создайте terraform.tfvars:**

```hcl
# Основные переменные
yc_cloud_id        = "your-cloud-id"
yc_folder_id       = "your-folder-id"
yc_organization_id = "your-org-id"

# Использование существующих ресурсов
existing_service_account_id = "aje1234567890abcdef"
existing_community_id       = "bc1234567890abcdef"
existing_project_id         = "bt1234567890abcdef"
existing_static_key_id      = "YCAJEabcdef1234567890"
```

3. **Примените конфигурацию:**

```bash
terraform init
terraform plan  # Проверьте, что новые ресурсы НЕ создаются
terraform apply
```

### Сценарий 3: Частичное использование существующих ресурсов

Можно использовать только часть существующих ресурсов:

```hcl
# terraform.tfvars
yc_cloud_id        = "your-cloud-id"
yc_folder_id       = "your-folder-id"
yc_organization_id = "your-org-id"

# Используем только существующий Service Account, остальное создаем новое
existing_service_account_id = "aje1234567890abcdef"
```

## 🔍 Как это работает

Этот подход использует **чистые Data Sources** вместо импорта:

1. Если переменная `existing_*_id` задана:
   - Terraform использует data source для получения информации о существующем ресурсе
   - Новый ресурс НЕ создается (count = 0)
   - Все скрипты и зависимости используют локальные переменные, которые указывают на существующий ресурс

2. Если переменная `existing_*_id` НЕ задана:
   - Terraform создает новый ресурс
   - Локальные переменные указывают на новый ресурс

3. Унифицированные локальные переменные:
   - `local.service_account_id`
   - `local.community_id`
   - `local.project_id`
   - `local.static_access_key_id`
   - `local.static_secret_key`

## 🔍 Проверка результатов

После применения конфигурации проверьте статус:

```bash
# Общая информация
terraform output datasphere_summary

# Статус ресурсов
terraform output import_status

# Конкретные ID
terraform output datasphere_project_id
terraform output service_account_id
terraform output community_id
```

Пример вывода:
```json
{
  "import_status": {
    "community": "existing",
    "project": "created", 
    "service_account": "existing",
    "static_key": "existing"
  }
}
```

## ⚠️ Важные замечания

### Особенности

1. **Static Keys** - для существующих Service Account секретный ключ будет недоступен (только access_key_id). Если вам нужен доступ к секретному ключу, создайте новый ключ через YC CLI.

2. **Зависимости** - убедитесь, что существующие ресурсы имеют правильные связи (Project должен быть в правильном Community).

3. **Права доступа** - Service Account должен иметь необходимые роли для DataSphere.

### Безопасность

- ✅ Все критичные ресурсы защищены `prevent_destroy = true`
- ✅ Sensitive данные помечены как `sensitive = true`
- ✅ Валидация ID предотвращает некорректные значения

## 🛠️ Устранение проблем

### Ошибка: "Resource not found"

Убедитесь, что указанный ID существует и доступен с вашими правами.

```bash
# Проверить существование сервисного аккаунта
yc iam service-account get <id>

# Проверить существование community
yc datasphere community get <id>

# Проверить существование project
yc datasphere project get <id>
```

### Ошибка: "Invalid resource ID format"

Проверьте формат ID в validation правилах в `variables.tf`.

### Ошибка: "Resource already exists"

Если вы пытаетесь создать ресурс с именем, которое уже существует, но не указали его ID как существующий, укажите соответствующий `existing_*_id`.

## 📞 Получение помощи

1. Проверьте логи: `terraform plan -detailed-exitcode`
2. Посмотрите состояние: `terraform state list`
3. Проверьте outputs: `terraform output -json`

---

**Преимущества этого подхода:**

- ✅ **Простота** - нет необходимости в сложных импортах
- ✅ **Надежность** - стандартные возможности Terraform
- ✅ **Гибкость** - можно комбинировать новые и существующие ресурсы
- ✅ **Безопасность** - существующие ресурсы не модифицируются
- ✅ **Совместимость** - работает с любой версией Terraform 